## Faster-Whisper の挙動と AD_FILTER パラメタの取り扱い

設定ファイル等で VAD_FILTER を true/false に設定可能になっているが、
VAD なしで Whisper を使う運用はまず考えられないことから、
設定ファイル等での指定は廃止して、ファイル入力/ストリーム入力
どちらの場合も false 固定で処理していいのではないか。

Faster-Whisper 本来の動作では、内蔵 VAD のチャンク分割で無音区間を挟んで
連続したチャンクを、連続した無音区間を取り除いた発言として認識処理するらしい。

VADのチャンク分割:
```
発言Aのチャンク: 今日は
無言区間
発言Bのチャンク: 雨降るのかな？
```

Whisper の認識結果:
```
今日は雨降るのかな？
(開始タイムスタンプは発言Aのチャンクの開始タイムスタンプ)
```

この際、発言Aのチャンクの 「今日は」が認識できず無音として認識された場合、
「雨降るのかな」 の開始タイムスタンプが発言Aの開始タイムスタンプになってしまう。

期待するのは「雨降るのかな」の開始タイムスタンプが
発言Bのチャンクの開始タイムスタンプになることである。

そこで、あえて内蔵 VAD を使わず、独自に VAD 機能を実装し、
発言チャンクごとに Whisper を発火させる。
この際、Whisper が文脈を理解できるように、
プロンプトとして直前の発言内容を Whisper に渡すようにする。

つまり、Faster-Whisper には VAD_FILTER = false
で、内蔵 VAD は使わず、音声認識に専念してもらう。

したがって、設定ファイル、CLIオプション引数から VAD_FILTER
を廃止して、内部では VAD_FILTER = true 固定で取り扱うように
設計する。
