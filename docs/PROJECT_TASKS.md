# タスクリスト (Task List)

## 進行中フェーズ



### Phase 14: 統合パイプラインアーキテクチャの実装 (コーディングフェーズ)

Phase 13 で策定した詳細設計（`detailed_design.md`）に基づき、新しいキューベース・リアルタイム進行アーキテクチャを実装します。

#### 14.1 オーケストレーターとプロデューサー層の構築
- [x] 14.1.1 `pipeline_supervisor.py` の作成
  - `PipelineSupervisor` クラスの実装（全体例外捕捉、CLI向けFacade）
  - CUDA OOM 等のランタイムエラーからの安全なリカバリ処理の実装
  - ステータスクリア機能 (`reset()` メソッド) によるライフサイクル管理の実装
- [ ] 14.1.2 `audio_producers.py` の作成と課題修正
  - [x] `AudioChunkQueue` (非同期セーフキュー) の実装
    - `asyncio.Queue(maxsize=maxsize)` による Backpressure（バックプレッシャー）制御
    - キューが満杯時は投入元（Producers）を非ブロッキングでブロック（`await queue.put`）し、メモリ過剰消費（OOM）を防止
  - [x] `StreamAudioProducer` の実装
    - 外部から `feed_chunk(chunk, is_speech)` された音声バイト列をそのまま（パススルーで）キューへ供給
    - 終了時に `stop()` を呼ぶことで、終了センチネル `None` を安全にキューへ投入
  - [x] `FileAudioProducer` の実装
    - ノイズ除去（RNNoise等）が有効な場合、`AudioDenoiser` を用いて一時クリーンファイル（`*_clean_temp.wav`）を非同期スレッド（`asyncio.to_thread`）で生成
    - `asyncio.create_subprocess_exec` で非同期 FFmpeg プロセス（`pcm_s16le`, モノラル, 指定レート）を起動し、音声データを逐次デコード
    - デコード出力を `chunk_size_ms`（デフォルト100ms ＝ 16kHz時3200バイト）単位に厳密にスライスしながら、ループでキューへ順次投入
    - キャンセル・エラー・終了時に FFmpeg 子プロセスを確実に terminate/wait し、一時クリーンファイルを自動削除するクリーンアップ（`_cleanup()`）を実装
  - [x] `tests/test_audio_producers.py` の作成とテスト検証
    - [x] `test_audio_chunk_queue_basic`: 基本的なデータ投入、取得、qsize 挙動の検証
    - [x] `test_audio_chunk_queue_backpressure`: キュー満杯時に put が適切にブロックされ、get されたら再開される Backpressure 制御の検証
    - [x] `test_stream_audio_producer`: ストリーミングデータのパススルーと終了通知の検証
    - [x] `test_file_audio_producer_without_denoise`: デノイズ無効時の FFmpeg 非同期プロセス経由のデコードと100ms境界での正確なチャンク分割（不完全チャンクの余り処理、終了センチネル None 投入を含む）の検証
    - [x] `test_file_audio_producer_with_denoise`: デノイズ有効時に RNNoise 前処理（一時ファイル生成）を経てから、その一時ファイルを FFmpeg に渡して非同期にキューへ流す全体の連動テストの検証
  - [ ] **指摘問題点に対する対処（タスク14.1.2 の堅牢化課題）**
    - [x] **課題A: キュー容量制限の動的化**
      - `AudioChunkQueue` の `maxsize=100` ハードコーディングを廃止。1チャンクの時間長とサンプリングレートから算出されるバイト数、および総蓄積目標秒数に基づき動的に `maxsize` を受け取るように改修する
    - [ ] **課題B: 動画リマックス（音声トラック差し戻し）を見据えた一時ファイル（ノイズ除去済み音声）のライフサイクル再設計** (⚠️ 現状確認の結果: タスクリストは [x] となっていたが、実際のコード `audio_producers.py` にはプロパティ定義やクリーンアップ移譲ロジックが未実装であり、ドキュメントと実情に乖離があることを確認。要修正。)
      - **背景**: ファイル入力（動画）時、文字起こし完了後に「ノイズが除去された綺麗な音声トラック」を元の動画に書き戻すリマックス（合成）処理が必要になる。そのため、ノイズ除去済み一時ファイル（`*_clean_*.wav`）を途中で勝手に削除してはならない。
      - **対策**:
        1. `FileAudioProducer` が勝手に一時ファイルを消去せず、ファイルパスを `producer.denoised_audio_path` プロパティ等で外部（オーケストレーター）に公開できるように設計変更する。
        2. 一時ファイルの最終クリーンアップ（削除）所有権をプロデューサー単体から、上位の `PipelineSupervisor` または統合パイプラインへ引き上げ、文字起こし ➔ リマックス（合成）のすべての行程が完全に終了した段階で、オーケストレーターが一括して消去するようにライフサイクルをリファクタリングする。
        3. 最初のノイズ除去時に生成したファイルを最後まで使い回すことで、ディスク書き出し（I/O）を最小限（全行程で1回のみ）に抑え効率を最大化する。
    - [x] **課題C: プロセス終了時のタイムアウトおよび強制停止（SIGKILL）の導入**
      - `_cleanup()` 内で `self._ffmpeg_process.terminate()` 後に一定時間（例: 0.5秒） `wait()` をタイムアウト監視。タイムアウトした場合は `kill()` (SIGKILL) を呼び出し、プロセス詰まりによる非同期イベントループの永続フリーズを防止する
    - [x] **課題D: FFmpeg のエラーログ伝播と異常終了検知**
      - `stderr=DEVNULL` を廃止し、`asyncio.subprocess.PIPE` 等で stderr をバッファリング。FFmpeg が異常終了（exit code != 0）した際、サイレントに文字起こしを空にするのではなく、エラー詳細を含む `RuntimeError` を送出しコールバックへ適切にエラー通知する
    - [x] **課題E: サンプリングレート仕様 of プロトコル整合性**
      - 16000Hz (Whisper想定) 以外のモデル（24kHzや48kHz等）が将来混在した際にも矛盾が生じないよう、Producer が要求サンプリングレートを引数で受け取る、または設定から動的に解決するインターフェース設計へ見直す

- [ ] **14.1.3 オーケストレーターとプロデューサーにおける堅牢化課題（課題A、課題B）の実装修正**

  - **14.1.3.1 課題A: `AudioChunkQueue` の `maxsize` 動的算出ロジックの実装**

    - [ ] **14.1.3.1.1 テスト整備: `AudioChunkQueue` の `maxsize` 動的算出に対応するテストの準備**
      - **目的**: `AudioChunkQueue` の `maxsize` 動的算出ロジックが正しく機能することを検証するためのテストを準備する。
      - **作業内容**: 
        - 既存テスト `test_audio_chunk_queue_backpressure` を、新しい引数 (`chunk_duration_ms`, `sample_rate`, `buffer_target_seconds`) を渡すように修正する（この時点では修正後のコードがないため失敗する可能性があるが、テスト準備として実施）。
        - `test_audio_chunk_queue_dynamic_maxsize` メソッドを追加し、異なるパラメータで `maxsize` の算出が正しいことを検証する単体テストのスケルトンを作成する。
    - [ ] **14.1.3.1.1-セルフチェック (テスト整備)**:
      - **観点5: テスト妥当性・網羅性**: 新規追加および更新したテストのスケルトンが、maxsizeの動的算出ロジックを適切に網羅し、エッジケースをカバーできるように準備されているか。テストランナーによって認識されるか。

    - [ ] **14.1.3.1.2 コード修正: `src/audio_transcriber/audio_queues.py` の `AudioChunkQueue.__init__` メソッドの変更**
      - **目的**: `AudioChunkQueue` の `maxsize` を動的に算出するための引数を受け取り、ロジックを実装する。
      - **対象ファイル**: `src/audio_transcriber/audio_queues.py`
      - **作業内容**: `__init__` メソッドのシグネチャと内部ロジックを変更し、`maxsize = int(buffer_target_seconds * sample_rate / (chunk_duration_ms / 1000 * sample_rate))` を基本とした算出ロジックを実装する。

    - [ ] **14.1.3.1.3 テスト実施と検証**: 
      - **目的**: コード修正が正しく行われ、テストが成功することを確認する。
      - **作業内容**: 
        - 修正されたコードに対して、`14.1.3.1.1 テスト整備` で準備したテスト（`test_audio_chunk_queue_backpressure` と `test_audio_chunk_queue_dynamic_maxsize`）を実行し、全て成功することを確認する。

    - [ ] **14.1.3.1.4 セルフチェック (コード修正とテスト結果)**:
      - **観点1: 開発ルール適合性**: `AGENTS.md` に準拠したコーディング規約（PEP8, 型ヒントなど）が守られているか。
      - **観点6: 簡潔性 (KISS)**: コードが簡潔で理解しやすいか。不要な複雑さがないか。
      - **観点9: ドキュメント同期**: 変更内容が適切にドキュメント化されているか（例: Docstringの更新）。
      - **観点5: テスト妥当性・網羅性**: 既存のテストが、変更後も期待される動作を適切に検証しているか。カバレッジが低下していないか。新規テストが意図通り機能しているか。


  - **14.1.3.2 課題B: `FileAudioProducer` の `denoised_audio_path` プロパティ実装とクリーンアップ権限の引き上げ**

    - [ ] **14.1.3.2.1 テスト整備: `test_file_audio_producer_with_denoise` の修正と `denoised_audio_path` プロパティの検証準備**
      - **目的**: `denoised_audio_path` プロパティの検証と、一時ファイル削除ロジック除去による影響を確認するためのテストを準備する。
      - **対象ファイル**: `tests/test_audio_producers.py`
      - **作業内容**: `test_file_audio_producer_with_denoise` テストにおいて、`denoised_audio_path` プロパティの値をアサートする準備を行う。また、テスト終了後も一時ファイルが存在することを確認するアサーションを追加する（このテスト単体では削除されないことを確認）。
    - [ ] **14.1.3.2.1-セルフチェック (テスト整備)**:
      - **観点5: テスト妥当性・網羅性**: 既存のテストが、変更後も期待される動作を適切に検証できるように準備されているか。`denoised_audio_path` の公開が正しく行われ、かつ一時ファイルが削除されていないことを確認するためのアサーションが追加されているか。

    - [ ] **14.1.3.2.2 コード修正: `src/audio_transcriber/audio_producers.py` の `denoised_audio_path` プロパティ追加と一時ファイル削除ロジックの除去**
      - **目的**: ノイズ除去済み一時ファイルのパスを外部に公開するプロパティを実装し、同時に `FileAudioProducer` から一時ファイルの削除責務を除去する。
      - **対象ファイル**: `src/audio_transcriber/audio_producers.py`
      - **作業内容**: `FileAudioProducer` クラスに `@property` デコレータを用いた `denoised_audio_path` を追加し、一時ファイルのパスを保持・公開するように修正する。ノイズ除去が行われない場合は `None` を返す。また、`FileAudioProducer` クラスの `_cleanup()` メソッドから、ノイズ除去済み一時ファイルを削除するロジックを完全に削除する。

    - [ ] **14.1.3.2.3 コード修正: `src/audio_transcriber/pipeline_supervisor.py` での一時ファイル削除のプレースホルダー設置**
      - **目的**: `PipelineSupervisor` が一時ファイルの削除責務を将来的に持つためのプレースホルダーを設置する。
      - **対象ファイル**: `src/audio_transcriber/pipeline_supervisor.py`
      - **作業内容**: `PipelineSupervisor` クラスに、`FileAudioProducer` から受け取った `denoised_audio_path` を保持するためのリスト（例: `self._temp_denoised_files = []`）を追加し、`_cleanup()` メソッドにそのリスト内のファイルを削除するロジックのコメントを追加する。

    - [ ] **14.1.3.2.4 テスト実施と検証**: 
      - **目的**: コード修正が正しく行われ、テストが成功することを確認する。
      - **作業内容**: 
        - 修正されたコードに対して、`14.1.3.2.1 テスト整備` で準備したテスト（`test_file_audio_producer_with_denoise`）を実行し、成功することを確認する。

    - [ ] **14.1.3.2.5 セルフチェック (コード修正とテスト結果)**:
      - **観点1: 開発ルール適合性**: `AGENTS.md` に準拠したコーディング規約が守られているか。
      - **観点4: 疎結合性**: `FileAudioProducer` が一時ファイルのライフサイクル管理から切り離され、責務が適切に分離されているか。
      - **観点7: 副作用**: 意図しないファイルの削除やリソースリークが発生しないか。
      - **観点9: ドキュメント同期**: 変更内容が適切にドキュメント化されているか。
      - **観点3: 設計整合性**: `PipelineSupervisor` が一時ファイルのライフサイクルを管理する設計意図と整合しているか。
      - **観点5: テスト妥当性・網羅性**: 既存のテストが、変更後も期待される動作を適切に検証しているか。`denoised_audio_path` の公開が正しく行われ、かつ一時ファイルが削除されていないことを確認しているか。


  - [ ] **14.1.3.3 課題A・課題Bの修正に対する結合テストの追加・更新**

    - [ ] **14.1.3.3.1 テスト整備: 新規結合テストファイル作成とテストケースのスケルトン作成**
      - **目的**: プロデューサーとキューの連携、一時ファイルのライフサイクル管理を検証する結合テストを設置する。
      - **対象ファイル**: `tests/test_e2e_producer_lifecycle.py` (新規作成)
      - **作業内容**: 新しいテストファイルを作成し、デノイズが有効な `FileAudioProducer` を使用し、`AudioChunkQueue` へデータ供給から、パイプライン終了後の適切に一時ファイルが削除されているかまでを検証するテストケースのスケルトンを作成する。
    - [ ] **14.1.3.3.1-セルフチェック (テスト整備)**:
      - **観点5: テスト妥当性・網羅性**: 新規テストファイルがテストランナーによって認識され、テストのスケルトンが適切に作成されているか。

    - [ ] **14.1.3.3.2 結合テストケース `test_producer_denoise_file_lifecycle` の実装**
      - **目的**: 課題Aと課題Bの修正が統合されたシステム全体で正しく機能することを検証する。
      - **対象ファイル**: `tests/test_e2e_producer_lifecycle.py`
      - **作業内容**: `test_producer_denoise_file_lifecycle` を実装し、`AudioChunkQueue` の動的な `maxsize` と `FileAudioProducer` の一時ファイルパス公開が連携し、ノイズ除去プロセスを含むエンドツーエンドのシナリオで、一時ファイルのライフサイクルが正しく管理されていることを確認する。

    - [ ] **14.1.3.3.3 テスト実施と検証**: 
      - **目的**: 結合テストが正しく行われ、成功することを確認する。
      - **作業内容**: 
        - 修正されたコードに対して、`14.1.3.3.2 結合テストケース` を実行し、全て成功することを確認する。

    - [ ] **14.1.3.3.4 セルフチェック (結合テストと結果)**:
      - **観点2: 要件適合性**: 課題Aと課題Bで定義された要件（動的なmaxsize、一時ファイルのライフサイクル管理）が、結合テストで正しく検証されているか。
      - **観点5: テスト妥当性・網羅性**: 結合テストがエンドツーエンドのシナリオを適切にカバーし、期待される動作を検証しているか。カバレッジが向上しているか。


#### 14.2 コア・パイプラインと波形スライス抽出の実装
- [ ] 14.2.1 `vad_manager.py` の改修・新規作成
  - `VadManagerProtocol` の定義 (DI注入用インターフェース)
  - `StreamingVadManager` の実装（内部に `AudioRingBuffer` を保持）
  - VAD終端検知時の波形スライス抽出とバッファ破棄メカニズムの構築
- [ ] 14.2.2 `unified_pipeline.py` の作成
  - `UnifiedTranscriptionPipeline` の実装
  - キューからの取り出し、タイムコード管理、および「VAD波形抽出 ➔ 推論 ➔ 後処理 ➔ 文脈追加」の同期直列ループの制御
  - 処理レイテンシやバッファ残量等の稼働状況を監視し `on_metrics` コールバックで定期通知する仕組みの構築

#### 14.3 推論エンジンのDI化と、後処理・文脈モジュールの実装
- [ ] 14.3.1 `stt.py` の改修 (DI・プロバイダーパターン化)
  - `TranscriberProvider` (Protocol) の定義
  - 既存の推論ロジックを `FasterWhisperProvider` として整理し、VADチャンク単位の逐次推論メソッドへ特化
  - 推論エンジンの抽象化と Factory パターンによる DI 切り替え機能の実装
- [ ] 14.3.2 `postprocessor.py` の作成 (旧ファイルの統合・マイグレーション)
  - `PostProcessorProtocol` の定義 (DI注入用インターフェース)
  - `TextPostProcessor` クラスの実装（無音・無効破棄、重複除去、辞書置換、正規化、タイミング・余韻補正）
  - 既存の `sanitizer_core.py` と `postprocess.py` のロジックを本ファイルに統合し、旧ファイルを削除
- [ ] 14.3.3 `context_manager.py` の作成
  - `ContextManagerProtocol` の定義 (DI注入用インターフェース)
  - `ContextManager` クラスの実装（履歴テキストのFIFO管理、トークン・文字数溢れ制御、次チャンク用プロンプト生成）
  - 履歴クリアのための `reset()` メソッドの実装

#### 14.4 エントリーポイントの統合とクリーンアップ
- [ ] 14.4.1 `cli.py` の改修と出力モデル標準化
  - 出力データモデルを `RecognizedSegment` へ完全準拠させる
  - 呼び出しロジックを新設した `PipelineSupervisor` の Facade へ切り替え
- [ ] 14.4.2 テストコードの再整備 (MockProviderの導入)
  - `MockProvider` を作成・展開し、GPUレスで非同期キューやDIパイプラインの全体状態遷移テストを可能にする
  - 新規作成・分割した各コンポーネント（特にバッファスライス抽出や溢れ制御）に対応する単体テストの追加・更新
- [ ] 14.4.3 旧アーキテクチャファイルの削除
  - E2Eで動作確認後、不要となった旧パイプラインコード（`pipeline.py`, `streaming/core.py` 等）の削除・整理

## 未着手のフェーズ



### Phase 15: GPU/デバイス不要テスト基盤の整備
- [ ] 15.1 ダミー波形入力によるパイプライン状態遷移・コールバック発火の単体テスト拡充（全モジュール横断）
- [ ] 15.2 CPU 環境での完全実行検証（CI 導入への道を開く）

## 将来対応
- [ ] 音響イベント検知（叫び声・大音量ピーク・笑い声等の分類検知および即時通知）
