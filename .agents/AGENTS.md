# audio-transcriber 開発ルール

## 1. コーディング規約
- **スタイル**: PEP 8 / Google Python スタイルガイド準拠。
- **命名**: クラス `PascalCase`, 関数/変数/ファイル `snake_case`, 定数 `UPPER_SNAKE_CASE`
- **フォーマット**: 4スペースインデント。行長制限は Ruff に任せる（pyproject.toml 設定準拠）。ただし、Ruff が適切に警告・改行できるよう、Docstring やコメント内では適宜カンマや空白を挟む。

## 2. 計画書 / Issue / ブランチ / PR 規約
- **優先順位**: Git・ブランチ運用は本プロジェクト規約（Issue連動ブランチ）を最優先適用する。
- **Issue & ブランチ**: `gh issue create` ➔ ブランチ名 `issue-<Issue番号>/<機能名>`
- **PR & マージ**: `gh pr create` (本文に `Closes #<Issue番号>` 明記) ➔ スカッシュマージ (`gh pr merge --squash`)
- **計画書の作成**: 複雑な変更時は計画書（`implementation_plan.md` / `task.md`）を作成し、作業タスクリストで進捗を管理する。

## 3. 規模制限 & 可読性 (KISS原則)
- **ファイルサイズ**: 1ファイル最大 300 行以下（目標 200 行以下）。超過時は単一責任原則に従いモジュール分割。
- **シンプルさ**: ワンライナーの詰め込みや過度に難解なロジックを避け、読みやすさを優先する。

## 4. Docstring & コメント
- 全モジュール/クラス/公開関数に Google スタイル Docstring (日本語) を記述。
- **サマリー行**: 1行目に簡潔な要約を記述。空行を挟んで詳細を記載。
- 関数 Docstring 必須項目: 概要, `Args`, `Returns`, `Raises`（発生しうる例外がある場合）

## 5. 定数一元管理 (ハードコード禁止)
- 数値・文字列・パス・モデル名・拡張子等の定数はコード内に直接ハードコードせず、設定クラス（`Config` 等）や定数モジュールに一元管理する。

## 6. 例外処理 & エラーハンドリング
- `try-except` 内のスコープは最小限に留め、ログ記録 (`logger.error`) を中心とする。リカバリロジックは例外ブロック外に分離する。
- CLI 実行時のユーザー向けエラー表示（Rich等）と内部ログを適切に区別する。

## 7. 設計思想 & パターン
1. **疎結合性 & 単体実行性**:
   - ノイズ除去 (`denoise`)、文字起こし (`transcribe`)、字幕生成 (`srt`) は明確に責務を分離し、パイプライン経由だけでなく単体でも呼び出し・テスト可能にする。
2. **Strategy ＋ DI (Protocol活用)**:
   - 音声処理や推論モデル等のプロバイダー処理は `Protocol` で抽象化しコンストラクタ注入（モック化・差し替え容易化）を行う。
3. **Facade**:
   - パイプラインオーケストレーター (`pipeline.py` 等) は外部に対してシンプルな単一インターフェースのみを公開する。
4. **リソース遅延ロード & ライフサイクル管理**:
   - DeepFilterNet や Whisper などの重いモデルや VRAM/CPU リソースはモジュール読み込み時ではなく処理実行直前にロード（Lazy Load）し、不要時は安全に解放する。
5. **非破壊処理 (Immutability)**:
   - 入力音声ファイルは常に読み取り専用とし、上書きや削除を行わない。生成物は指定ディレクトリへ新規出力する。
6. **互換性レイヤー (Shim / Adapter) の隔離**:
   - ライブラリ間のバージョン差異吸収やパッチ処理はコアロジック内に混在させず、専用の互換モジュール（`compat.py` 等）に隔離する。
7. **DaVinci Resolve 親和性・出力仕様の安定性**:
   - 字幕ファイル (`.srt`) は DaVinci Resolve のインポート仕様（ミリ秒表記 `00:00:00,000`、UTF-8、改行コード LF）を厳守する。
   - 出力命名規則（`{stem}_clean.wav`, `{stem}.srt`）の規則性を維持する。
8. **コア層の同期維持と非同期・スレッド分離の局所化**:
   - パイプラインおよび各モジュール（ノイズ除去、文字起こし、後処理、出力）のコアロジックは同期的な単一フローとして実装し、可読性・単体実行性・テスト容易性を最優先する。
   - UI 描画（プログレスバー等）の非ブロッキング化や将来的なバッチ並行処理は、コア内部ではなく呼び出し側（CLI / 外層オーケストレーター）でスレッド分離やコールバック注入を行う。
   - GPU / VRAM を消費するモデル推論処理は無秩序に並行実行させず、リソース枯渇を防ぐため逐次実行または制御されたワーカーキューで管理する。
9. **コード・機能の重複排除 (DRY原則)**:
   - 同一の変換ロジック、正規表現パターン、ファイル命名・I/O 処理、定数は複数箇所に分散させず、共通モジュール・ユーティリティ・Config に一元化する。

## 8. コード品質 & 自動化ツール
- Python 3.12+ 準拠、全関数・クラスに厳格な型注釈を付与。
- 型チェック: `uv run basedpyright` （エラー 0 件）
- リント / フォーマット: `uv run ruff check --fix .` および `uv run ruff format .`
- 一括自動検証: `uv run pre-commit run --all-files` （リント・フォーマット・型チェック・テストを一括実行）

## 9. テストコード (`tests/`) 規範 & カバレッジ基準
- **構造化**: モジュール単位で `tests/test_*.py` に配置。テスト共通フィクスチャは `conftest.py` に集約。
- **記述スタイル**: AAA (Arrange-Act-Assert) パターンを徹底。テストロジック内に複雑な分岐やループを書かない。
- **モック活用方針**: 重いモデル（DeepFilterNet, faster-whisper）や外部 CLI（ffmpeg, ffprobe）の処理は `unittest.mock` を用いて高速・決定論的に検証する。
- **カバレッジ管理基準**:
  - **必須カバー対象**: 具象プロバイダ実装のパラメータ伝達・I/Oフロー、外部CLI失敗時の例外ハンドリング（`CalledProcessError` ➔ `RuntimeError`）、互換レイヤーのフォールバック、バリデーション境界値。
  - **正当な除外対象**: `if __name__ == "__main__":` 等の別プロセス起動が不要な定型コードのみ `pyproject.toml` の `exclude_lines` で明示除外する。
  - 詳細は `docs/testing_and_coverage.md` を参照。
- **テスト実行**: `uv run pytest --cov=audio_transcriber --cov-report=term-missing` でパスし、高いカバレッジを維持することを確認。

### 10. セルフチェック
作業完了前に以下の観点でセルフチェックを実施。
チェックにパスするまで自律的に再修正。
セルフチェック結果を報告。
- **セルフチェックの観点**: 
1. 開発ルール適合性
2. 要件適合性
3. 設計整合性
4. 疎結合性
5. テスト妥当性・網羅性 (エッジケースカバー評価)
6. 簡潔性 (KISS)
7. 副作用
8. パフォーマンス
9. ドキュメント同期
